# Get an API key at https://platform.openai.com/api-keys
# A paid OpenAI account is required (small credit goes a long way — gpt-4o-mini
# runs the example dataset at well under a cent per eval).
OPENAI_API_KEY=sk-...
